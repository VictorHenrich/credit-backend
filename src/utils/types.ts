export enum EmployeeLoanStatusType {
  SUCCESS = 'success',
  ERROR = 'error',
  PROGRESS = 'progress',
}

export enum ResponseTypes {
  SUCCESS = 'success',
  ERROR = 'error',
  UNAUTHORIZED = 'unauthorized',
}
